# Generated by `pmstate init` from pmstate.yaml. Edit freely.
"""Run a one-shot prompt against the tree."""
import sys
from pathlib import Path

from pmstate import ClaudeHarness
from tree import build_tree

prompt = sys.argv[1] if len(sys.argv) > 1 else None
harness = ClaudeHarness(tree=build_tree(), root_dir=Path(__file__).parent, watch=False)
result = harness.run(prompt)
if result is not None:
    print(result)
