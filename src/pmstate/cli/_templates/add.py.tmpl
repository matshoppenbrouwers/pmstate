# Generated by `pmstate init` from pmstate.yaml. Re-run `init --upgrade`
# to refresh after changing the spec — manual edits to this file will be
# overwritten on upgrade.
#
# Tip: `pmstate append PATH --type TYPE --data '{...}'` performs the
# same write without code generation. This script remains for users
# who prefer typed sub-commands per event.
"""Append events to $tree_name. One sub-command per declared event type."""
from __future__ import annotations

import argparse
from pathlib import Path

from pmstate import Event, append_event

LEAVES: dict[str, str] = $leaves_dict


def main() -> None:
    p = argparse.ArgumentParser(prog="add", description="Append an event to a Log leaf.")
    sub = p.add_subparsers(dest="cmd", required=True)

$subparsers

    args = p.parse_args()

$dispatch


if __name__ == "__main__":
    main()
