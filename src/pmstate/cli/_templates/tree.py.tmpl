# Generated by `pmstate init` from pmstate.yaml. Edit freely.
"""Tree definition for $tree_name."""
from __future__ import annotations

from pathlib import Path

from pmstate import Log, Node, Table, Tree

$reducers_imports
$views_imports

_S = Path(__file__).parent / "state"


def build_tree() -> Tree:
    return Tree(
        "$tree_name",
        root=Node(
            "$root_name",
            children=[
$nodes_block
            ],
        ),
    )
